[package]
name = "data_collection"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4.41"
dotenv = "0.15.0"
futures = "0.3.31"
lazy_static = "1.5.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.142"
tokio = { version = "1.38", features = ["full"] }
url = "2.5.4"

# WebSocket client over Rustls (uses system roots)
tokio-tungstenite = { version = "0.20", features = ["rustls-tls-native-roots"] }

# Redis with Rustls TLS over Tokio
redis = { version = "0.25", features = ["tokio-rustls-comp", "tls-rustls"] }

# Postgres + chrono + Rustls TLS
tokio-postgres = { version = "0.7", features = ["with-chrono-0_4"] }
postgres-types = { version = "0.2", features = ["with-chrono-0_4"] }
postgres_rustls = "0.12"
rustls = "0.23"
rustls-native-certs = "0.7"

[[bin]]
name = "websocket"
path = "src/bin/websocket.rs"

[[bin]]
name = "trigger"
path = "src/bin/trigger.rs"
